import { IRenderConfig } from './types';

const renderConfig: IRenderConfig = {
    emptyCellColor: '#525053',
    berryColor: '#A00034',
    snakeHeadColor: '#FA0556',
    snakeTailColor: '#A00034',
    backgroundColor: '#353336',
};

export default renderConfig;